{
  "name": "googlebooks-app",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node app.js && npm run client:install && npm run client:build",
    "server": "nodemon app.js",
    "client": "npm --prefix client run start",
    "client:install": "npm --prefix client install react-scripts",
    "client:build": "npm --prefix client run build",
    "dev": "cross-env NODE_ENV=development concurrently \"npm run server\" \"npm run client\""
  },
  
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
